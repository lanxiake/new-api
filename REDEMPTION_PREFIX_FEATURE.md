# 兑换码前缀和每用户限制功能

## 功能概述

为兑换码管理系统添加了两个新功能：
1. **自定义前缀**：允许为兑换码设置自定义前缀，便于分组管理
2. **每用户限制**：可以设置每个用户只能使用一次该前缀的任意兑换码

## 数据库变更

### Redemption 表新增字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `prefix` | varchar(20) | 兑换码前缀，可选，用于分组管理 |
| `one_per_user` | boolean | 是否限制每个用户只能使用一次该前缀的兑换码 |

### 新增 RedemptionUsage 表

用于记录用户使用兑换码的历史，支持每用户限制功能。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | int | 主键 |
| `user_id` | int | 用户ID |
| `redemption_id` | int | 兑换码ID |
| `prefix` | varchar(20) | 兑换码前缀（冗余存储，用于快速查询） |
| `redeemed_time` | bigint | 使用时间 |

**索引**：
- `idx_user_prefix` (user_id, prefix) - 复合索引，用于快速查询用户是否使用过某前缀的兑换码

## API 变更

### POST /api/redemption/ (创建兑换码)

**新增请求参数**：
```json
{
  "name": "VIP会员兑换码",
  "quota": 100000,
  "count": 10,
  "expired_time": 1735660800,
  "prefix": "VIP2024",        // 新增：前缀（可选，2-20字符）
  "one_per_user": true         // 新增：是否限制每用户使用一次
}
```

**兑换码生成规则**：
- 有前缀：`PREFIX-UUID` (例如：`VIP2024-a1b2c3d4-e5f6-7890-abcd-ef1234567890`)
- 无前缀：`UUID` (例如：`a1b2c3d4-e5f6-7890-abcd-ef1234567890`)

### PUT /api/redemption/ (更新兑换码)

**新增可更新字段**：
- `prefix` - 前缀
- `one_per_user` - 每用户限制

### POST /api/user/topup (用户兑换)

**新增错误消息**：
- `redemption.prefix_used` - "您已使用过该系列的兑换码"

## 前端变更

### 创建/编辑兑换码界面

在"基本信息"卡片中新增两个字段：

1. **前缀** (prefix)
   - 输入框，可选
   - 占位符：`例如：VIP2024（可选）`
   - 说明：设置前缀后，兑换码格式为：前缀-随机码

2. **每用户限制** (one_per_user)
   - 开关组件，默认关闭
   - 说明：开启后，每个用户只能使用一次该前缀的任意兑换码

### 兑换码列表表格

新增两列：

1. **前缀** - 显示兑换码前缀，无前缀显示 "-"
2. **每用户限制** - 显示是否启用限制
   - 启用：绿色标签 "是"
   - 未启用：灰色标签 "否"

## 业务逻辑

### 兑换流程

1. 用户提交兑换码
2. 验证兑换码有效性（存在、未使用、未过期）
3. **新增**：如果兑换码设置了 `one_per_user=true` 且有前缀：
   - 查询 `redemption_usages` 表，检查该用户是否已使用过相同前缀的兑换码
   - 如果已使用过，返回错误："您已使用过该系列的兑换码"
4. 增加用户额度
5. 标记兑换码为已使用
6. **新增**：如果兑换码有前缀，在 `redemption_usages` 表中记录使用历史

### 使用场景

#### 场景1：VIP会员兑换码

```
前缀：VIP2024
每用户限制：开启
生成数量：100

结果：
- 生成100个兑换码：VIP2024-xxx1, VIP2024-xxx2, ..., VIP2024-xxx100
- 每个用户只能使用其中一个兑换码
- 防止用户重复兑换VIP权益
```

#### 场景2：活动兑换码

```
前缀：SPRING2024
每用户限制：开启
生成数量：1000

结果：
- 生成1000个兑换码用于春季活动
- 每个用户只能参与一次活动
- 即使用户获得多个兑换码，也只能使用一次
```

#### 场景3：普通兑换码（无限制）

```
前缀：（留空）
每用户限制：关闭
生成数量：50

结果：
- 生成50个普通兑换码
- 用户可以多次使用不同的兑换码
- 适用于无限制的充值场景
```

## 国际化

### 新增翻译键

| 键 | 中文 | 英文 |
|----|------|------|
| `redemption.prefix_length` | 兑换码前缀长度必须在2-20之间 | Redemption code prefix length must be between 2-20 |
| `redemption.prefix_used` | 您已使用过该系列的兑换码 | You have already used a redemption code from this series |

## 数据库兼容性

所有变更已确保兼容：
- ✅ SQLite
- ✅ MySQL 5.7.8+
- ✅ PostgreSQL 9.6+

## 迁移说明

### 自动迁移

系统启动时会自动执行以下迁移：
1. 在 `redemptions` 表中添加 `prefix` 和 `one_per_user` 字段
2. 创建 `redemption_usages` 表
3. 创建必要的索引

### 现有数据

- 现有兑换码的 `prefix` 字段为空字符串
- 现有兑换码的 `one_per_user` 字段为 `false`
- 不影响现有兑换码的使用

## 测试建议

### 功能测试

1. **创建带前缀的兑换码**
   - 创建前缀为 "TEST2024" 的兑换码
   - 验证生成的兑换码格式为 `TEST2024-UUID`

2. **每用户限制测试**
   - 创建前缀为 "VIP" 且启用每用户限制的兑换码（生成2个）
   - 用户A使用第一个兑换码，应成功
   - 用户A尝试使用第二个兑换码，应失败并提示"您已使用过该系列的兑换码"
   - 用户B使用第二个兑换码，应成功

3. **无限制测试**
   - 创建不启用每用户限制的兑换码（生成2个）
   - 用户A使用第一个兑换码，应成功
   - 用户A使用第二个兑换码，应成功

4. **前缀验证测试**
   - 尝试创建前缀长度为1的兑换码，应失败
   - 尝试创建前缀长度为21的兑换码，应失败
   - 创建前缀长度为2-20的兑换码，应成功

### 数据库测试

在三种数据库上分别测试：
- SQLite
- MySQL
- PostgreSQL

验证：
- 表结构正确创建
- 索引正确创建
- 数据正确插入和查询

## 部署步骤

### 本地测试

1. 启动开发环境：
```bash
docker compose -f docker-compose.dev.yml up -d
```

2. 启动前端：
```bash
cd web/classic
bun install
bun run dev
```

3. 测试功能

### 生产部署

按照 CLAUDE.md 中的部署流程：

1. 本地构建后端：
```bash
docker build -f Dockerfile.build -t new-api:local-build .
```

2. 提取二进制并上传到服务器

3. 替换容器内二进制并重启

## 注意事项

1. **数据库迁移**：首次部署时会自动执行迁移，建议在低峰期部署
2. **向后兼容**：现有兑换码不受影响，可以正常使用
3. **性能影响**：每用户限制功能会在兑换时增加一次数据库查询，但已通过索引优化
4. **前缀唯一性**：前缀不强制唯一，可以创建多批相同前缀的兑换码

## 版本信息

- 实现日期：2026-05-24
- 分支：llm-link-v1.0
- 影响模块：
  - 后端：model/redemption.go, controller/redemption.go, i18n/
  - 前端：web/classic/src/components/table/redemptions/
  - 数据库：redemptions 表, redemption_usages 表（新增）
